# Changelog

The canonical changelog lives at the repository root: [../CHANGELOG.md](../CHANGELOG.md).

(This file previously duplicated a subset of entries, which drifted. It is now
a pointer only.)
