# CHRONOS v2.3 dependencies
# Pinned to compatible-release ranges (~=) for reproducibility.
# Historical dependency snapshot. Current install metadata lives in pyproject.toml.

mcp[cli]~=1.6
numpy~=2.2
